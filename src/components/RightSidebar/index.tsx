import React from "react";
import { InputSearch, Matches, Historial } from "..";

const RightSidebar = () => {
  return (
    <div>
      <InputSearch />
      <Matches />
      <Historial />
    </div>
  );
};

export default RightSidebar;
